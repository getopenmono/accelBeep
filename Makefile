# Makefile created by monomake, Lør 18 Feb 2017 17:21:18 CET
# Project: accelBeep

MONO_PATH=$(shell monomake path --bare)
include $(MONO_PATH)/predefines.mk

TARGET=accelBeep

include $(MONO_PATH)/mono.mk
